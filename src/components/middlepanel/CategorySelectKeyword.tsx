
// CategorySelectKeyword.tsx (이 행 삭제 금지)
import React from 'react';

// 더 이상 필요하지 않은 중복 파일이므로 빈 컴포넌트로 유지
// CategoryResultPanel.tsx를 대신 사용
const CategorySelectKeyword: React.FC = () => {
  return null;
};

export default CategorySelectKeyword;
