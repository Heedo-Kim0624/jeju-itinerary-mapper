
import React from 'react';

// This component is intentionally left empty as we're removing the day selector from the map
const DaySelector: React.FC<any> = () => {
  return null;
};

export default DaySelector;
